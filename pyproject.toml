[project]
name = "malange"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "the world's spirit", email = "anonymous@anonymous.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
malange = "malange:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
