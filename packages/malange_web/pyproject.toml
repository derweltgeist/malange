[project]
name = "malange-web"
version = "1.0.0.dev1"
description = "Malange components required for web development."
readme = "README.md"
authors = [
    { name = "derweltgeist", email = "anoki.blazed@gmail.com" }
]
requires-python = ">=3.11"
dependencies = ["malange_core"]

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[tool.uv.sources]
malange-core = { workspace = true }
